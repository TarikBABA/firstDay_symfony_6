{% extends "template.html.twig" %}
{% block title %}
	Users
{% endblock %}
{% block body %}
	{% include ("fragments/date_day.html.twig")%}
	<div class="alert alert-warning">
		<a href="{{ path('tab', {nb : 7}) }}">Stats Table</a>
	</div>
	{% block pageTitle %}
		Users
	{% endblock %}
	<table class="table">
		<tr class="table-active">
			<td>name</td>
			<td>firstName</td>
			<td>âge</td>
			<td>Message</td>
		</tr>
		{% for user in users %}

			<tr class="table-{% if loop.index0 is odd %}warning{% else %}danger{% endif %}">
				<td>{{user.firstName}}</td>
				<td>{{user.name}}</td>
				<td>{{user.age}}</td>
				<td>
					{{ render(controller('App\\Controller\\FirstController::sayHola',{firstName : user.firstName, name : user.name})) }}</td>


			</tr>
		{% endfor %}
	</table>
{% endblock %}
